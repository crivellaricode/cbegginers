#include <stdio.h>

int main(void)
{
    char myCharacter = 'S';
    char simbolChar = 64;

    printf("myCharacter = %c and ASCII = %d\n", myCharacter, myCharacter);
    printf("simbolChar = %c and ASCII = %d\n", simbolChar, simbolChar);
    return 0;
}
